name: personal_streaming_backend
description: Backend for the personal streaming service.
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  # Stage 1 uses only Dart built-in libraries.