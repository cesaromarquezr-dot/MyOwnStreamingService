# DDNS

Configure your router or a dedicated updater for the DDNS provider. The application accepts `DDNS_HOSTNAME` and `DDNS_PROVIDER` for status/configuration display. Do not put DDNS provider credentials in Flutter or source control; keep them in the router/updater's secret store.
