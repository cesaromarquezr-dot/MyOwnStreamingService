# Flutter client

Each Dart file contains a file-level documentation header. Major classes and public methods are documented inline. The UI is intentionally split into feature files so media, music, reviews, storage and server administration can evolve independently.
